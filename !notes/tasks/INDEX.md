# Investigation Notes Index

| Folder | Module | Summary |
|--------|--------|---------|
| [cli-process-list-no-reply/](cli-process-list-no-reply/) | webview/SettingsModal+CliProcessesModal, backend/processes (diagnosis only, no fix committed) | NOT A BUG: "CLI processes `-`, modal stuck on Loading..." was the first ~500ms. Sampling the real UI against the live daemon every 100ms showed `-` at 87/310/423ms and the full table at 533ms - `getServerInfo` answers synchronously while `listCliProcesses` shells out to PowerShell, and both leave the same `useEffect` one line apart, so the slower round trip read as a fault. Ruled out with evidence: daemon age/handler presence (discovery file + compiled bundle), `listCliProcesses` hanging (382ms on the compiled bundle), the `VS_ONLY` routing trap (not in the list), the server dropping requests under the modal's 3s polling (8/8 answered in 362-492ms via `hammer-live-daemon.js`), and a stale webview bundle. Two real defects found while checking and still open: `-` means both "still asking" and "could not look" (`liveProcesses` is null in each case, so normal latency is indistinguishable from a failed listing), and the client give-up deadline equals the server worst case (`REPLY_TIMEOUT_MS` 10s vs `PS_TIMEOUT_MS` 10s), so a PowerShell timeout guarantees the client first shows a confidently wrong "your daemon may be too old" |
| [e2e-full-run-2026-09-12/](e2e-full-run-2026-09-12/) | e2e/usage-indicator-integration, !notes/common/e2e-testing (no product code) | Full `yarn test:e2e`: **454 passed, 2 failed, 6 skipped, 10 did not run**, mock 333/333 green. `usage-indicator-integration.spec.ts:110` hung on a bare 30s test timeout while its two API-dependent siblings skipped correctly - that asymmetry is the tell, since reaching the skip needs BOTH frames of the two-phase `accountUsage` reply. Its `waitForFrame` attached a listener per call, so nothing listened between waits and two frames sharing a TCP read cost the second; the server writes them in one tick whenever the usage snapshot is warm, because both sends hang off the same promise. Fixed by buffering from construction and **consuming** the queue, plus a cap lowered 45s -> 25s (above the 30s test timeout it was dead code that deleted the diagnostic). The end-to-end failure was deliberately **not** claimed as reproduced: a red control (pre-fix, `--repeat-each=3`, warm) passed every iteration, so the mechanism is demonstrated in isolation while "this is what killed that run" stays a hypothesis - which also weakens the previous 20s -> 45s raise, blamed on a slow spawn the dropped frame explains just as well. `effort-thinking-integration.spec.ts:185` failed for the second day running (16/16 alone); `readConfig`'s mtime cache checked and killed as a candidate, still unexplained |
| [ask-submit-kills-daemon/](ask-submit-kills-daemon/) | backend/session, webview/ToolCall, e2e | FIXED: clicking **Submit** on an AskUserQuestion dialog disconnected every VS Code panel at once. `flushAskFollowUp` read the model's `questions` as an array, but the CLI delivers it as a **JSON string** - 2 of 2 such calls on this machine, so the feature was broken for every use - and `?.` guards `undefined`, not a wrong type, so `questions?.find` threw straight out of `ws.on('message')`. The daemon installs no `uncaughtException` handler (the dev server does), so one client's Submit killed the shared process and every client on it; corroborated on the user's own box by the live daemon's `startedAt` being 24s younger than the second dialog. The webview had parsed the string since `ask-dialog-string-input.spec.ts`, which is why the dialog *looked* fine - the guard existed on one side of the wire only. The process-level net was measured and **rejected as the fix**: on the dev server the same payload throws, is caught, and the answers are silently swallowed, so a net alone turns a crash into a dead dialog. Reproduced deterministically by putting a stub `claude.cmd` first on the daemon's `PATH` (`resolveClaudeBin()` uses `where`), which is the only way to control a model-authored payload. Open: whether the daemon should adopt the dev server's `uncaughtException` net |
| [cli-process-list/](cli-process-list/) | backend/processes(new)+cli+channel+session, webview/CliProcessesModal(new)+SettingsModal+shared/Modal+utils/time, e2e | DONE: the Settings "CLI launches" number is now a button opening a live list of every Claude CLI process on the server machine (pid, session, start time, uptime, CPU time, CPU %, memory). CPU % is an interval reading off a per-pid baseline, so it is `-` until a second sample rather than a false `0%`, and it is share-of-one-core (98.6% on a busy loop where Task Manager says 4.1% on 24 cores). Ownership matching has to accept a **child** of an owned pid, since Windows spawns the CLI through cmd.exe. Follow-up from the user within a minute of shipping: `CLI launches 0` opening a list of ten reads as a broken counter even though both numbers are right, so a "CLI processes" row now carries the machine-wide count beside it, fed by the same reply the list renders. Then a tree view grouping each CLI under the process that started it: a literal Process Explorer tree was measured and rejected (every CLI sits under a cmd.exe Argus itself inserts, and one real chain ran nine deep through yarn/tsx wrappers), so the walk skips shell wrappers, stops at the first meaningful ancestor, and keeps the full chain as hover text. Then a per-row terminate button (hover-revealed trash, like Session History delete): the client-supplied pid is guarded twice, by the live listing and by taskkill's own image-name filter, since otherwise anything that can open a WebSocket could have the server kill any process on the machine. Then Running + Last activity columns: mtime freshness was measured and rejected as a liveness signal (0 writes in 10s on a session that was mid-turn), so 'running' comes only from the server registry and is null/'-' for a foreign process, never 'no'. Then an idle-CLI watchdog (cliIdleTimeoutSec, 0=off) reaping this server's finished-but-idle CLIs to reclaim ~250MB each: it skips anything mid-turn, because killing a CLI whose transcript ends on an unanswered user message splices a permanent 'No response requested.' turn, and it keeps the session id so the next send resumes |
| [bg-task-elapsed-timer/](bg-task-elapsed-timer/) | backend/sessionState+cliHandler+channel, webview/reducer+types+ChatMessage+BackgroundTasksNote, e2e | DONE: a 45-minute CI poller left the screen frozen - the turn timer stopped at 57s and the note under it was static - and the transcript shows the user asking "текущий статус CI" by hand six minutes in; the note now counts up from the task's launch, which required `pendingBgTasks` to become a `Map<string, number>` because nothing in the process knew when a task started; the turn's own timer stays frozen on purpose (it measures the turn, not the task) and no launch timestamp means no clock at all, since a half-hour poller shown as "3s" is worse than no number - that direction is the second of the two new tests |
| [bash-caption-truncation/](bash-caption-truncation/) | webview/ToolCall+ToolCall.module.css | DONE: a Bash row's description rendered as four characters ("Обно…") beside a long `node -e` command and stayed four at any window width, because flex distributes the deficit in proportion to each item's content width so both shrink by the same ratio; fixed by naming the item meant to truncate (`.toolSummaryDesc { flex-shrink: 0; max-width: 45% }`) rather than `flex: 1 1 0` on the command, which would have pushed the `Out` link to the far right on short commands |
| [bg-turn-cause-marker/](bg-turn-cause-marker/) | backend/taskNotification(new)+cliHandler+sessions+channel, webview/reducer+types+ChatMessage+StreamingMessage+BackgroundNoticeBlock(new), e2e | FIXED: "there was no request from me, how can these turns be real" - hiding the `<task-notification>` prompt in the [previous round](bg-turn-completion-noise/notes.md) left the turn it starts with **no visible cause**, so a second answer appeared, worked, and closed in the same success green as one the user asked for. The prompt is now replaced rather than dropped (muted marker with the CLI's own summary plus an `output` link, live and on replay), and a turn that leaves tasks running gets a neutral timer, since green claims "nothing outstanding". The live half was built on the `user` event first and was **dead code with a passing test**: a probe against a real CLI showed that record never reaches stdout (three `user` events per cycle, all `tool_result`), so it rides the `system`/`task_notification` event and the earlier "dropped only by accident" explanation is [superseded](../common/cli-turn-boundaries.md). Second round, same day: the `✻ N` pill counts every Bash call, because the CLI raises `task_started` for all of them (a lone foreground `echo` produced one, measured), so it blinked on every command; membership now needs `run_in_background` on the input or the `Command running in background with ID:` line in the result, the latter being the only signal for a command the CLI backgrounds on its own. |
| [bg-turn-completion-noise/](bg-turn-completion-noise/) | backend/cliHandler+session+sessions+channel, webview/App+reducer+InputArea, e2e | FIXED: during a CI watch a finished turn with a green timer appeared every four minutes, read as "sessions keep ending early". The timers were **correct** (each notification turn really is ~16s; the 4-5 min gaps are the background wait), so the defect was elsewhere: **156** such turns in one session each rang the completion sound and raised an OS toast, the pending count was visible nowhere (the note lives only on the newest turn), and replay rendered 42 raw `<task-notification>` XML bubbles among 115 user messages. Alerts are now withheld only **mid-chain** - a turn leaving nothing pending still rings, since that is the "CI is green, all done" report - plus a live `✻ N` pill in the input area, and the pending set is reaped per CLI spawn instead of per send. The literal ask ("don't finish the turn until all bg finished") was refused: it is the [forever-spinner](background-waiting-forever/notes.md) that was deleted in August |
| [e2e-turn-cost-budgets/](e2e-turn-cost-budgets/) | e2e (7 spec files), no product code | FIXED: integration specs failing one or two at a time, a different pair each run, never on the assertion they exist for. All were budgeted when a turn cost "a few seconds"; a fresh session here now starts at **77,346 input tokens** (193KB `CLAUDE.md`), so one costs 5-20s of which 17.6s was a single wait for the first token. Three budgets can bind and raising the wrong one changes nothing - test timeout, assertion cap, and a hand-rolled `Date.now() + 15_000` no config can reach (it missed by 90ms). Open: whether to raise the project timeout once instead of per-spec, and that the real lever is `CLAUDE.md`'s size |
| [phantom-image-inject/](phantom-image-inject/) | backend/cliHandler+sessions, e2e | FIXED: bubbles the user never typed, one after every image `Read` (`[Image: original 2122x3135, displayed at 1354x2000…]`). The CLI writes user-role messages of its own and tags them `isSynthetic` on the stream / `isMeta` in the transcript; neither path checked. Suppressing on the *text* would have been wrong: an audit of 1,885 transcripts found 707 such records, of which only 99 are image notes and ~370 are slash-command expansions. Gated per **block**, since a `/skill` call with an image pasted alongside arrives as one record holding the CLI's text and the user's own image |
| [html-preview/](html-preview/) | webview/FileViewerModal, e2e | Exported `.html` documents opened as markup; they now render in the previewer inside a scriptless `<iframe srcDoc sandbox="">` with a Source/Preview toggle; an `offset` deliberately does **not** fall back to Source (the first shape did, which made the feature invisible on the very read that prompted it). Open: paths with Cyrillic segments never linkify (both regexes are ASCII `\w`), and scroll-to-line does nothing for any offset Read, since the source view renumbers the slice from 1 |
| [hyphen-in-filename-truncates-link/](hyphen-in-filename-truncates-link/) | webview/utils/filePath+markdown, e2e | FIXED: `...\credentials\.corp-account` linked `...\credentials\.corp` (a file that does not exist) in a code span, and in prose lost the backslashes around each dot - both regexes ended the filename with `\.\w+` and `\w` excludes `-`; suffix widened to `\.\w+(?:-\w+)*` in both, plus a leading-dot filename now matches on the unix/relative branches. Open: a final segment with no dot still links a shorter prefix ([common/markdown-file-paths.md](../common/markdown-file-paths.md)) |
| [image-preview-from-tool-result/](image-preview-from-tool-result/) | backend/toolResult+sessions+cliHandler+session+channel, webview/PreviewContext+ToolCall, e2e | FIXED: an image the agent had read previewed as `Error reading file: ENOENT` once the agent moved the file, because the preview re-read the path off disk and ignored the image the tool returned - which was in the message all along. Tool results now carry `[image image/jpeg 174 KB]` instead of base64 on both the live and replay paths, and the bytes are fetched per click from the transcript by `tool_use_id` (`readToolImage`), so a moved or deleted file still previews and the reported session's replay payload went **24.2MB -> 0.8MB** ([common/large-tool-payloads.md](../common/large-tool-payloads.md)) |
| [background-waiting-forever/](background-waiting-forever/) | webview/reducer+ChatMessage+WorkingIndicator+BackgroundTasksNote, e2e | FIXED: "Waiting N background tasks" ran forever once a task never ended (a browser held open for CDP, a watcher). A turn with pending tasks re-armed a synthetic streaming state instead of finishing, so the spinner waited on a *later* turn that never came, and the completion sound + notification were suppressed for every such turn. Upstream has no such state (checked in the CLI bundle: the turn ends, `/tasks` lists the pending set passively), so the state was deleted and replaced by a static note on the finished message |
| [empty-1s-turn/](empty-1s-turn/) | backend/cliHandler+session+sessionState, e2e | FIXED: a send sometimes committed an empty assistant message with a ~1s timer, the real answer arriving 5-15s later as a second one. The CLI answers its own orphaned-background-task notification at `--resume` startup, before the user's queued message, and that turn's `result` was taken as the end of the user's turn ([common/cli-turn-boundaries.md](../common/cli-turn-boundaries.md)); also stabilised two integration specs that were racing the model ([common/e2e-testing.md](../common/e2e-testing.md)) |
| [stale-daemon-deploy/](stale-daemon-deploy/) | deploy (no code change) | Two bugs reported fresh (`Waiting N background tasks` under a finished turn; a `1s` empty turn before the real answer) were both already fixed here - the daemon was serving from `local.argus-0.0.88` built 5 days earlier, and `daemonIdleMs: 360000000` (100h) means a stale build is never rotated out. Transcript proof that a `<task-notification>` was dequeued ahead of the user's message; packaged + installed 0.0.89, smoke-probed the new build on a private port before the swap, and checked `getActiveSessions` so no one else's turn was killed |
| [usage-limits-indicator/](usage-limits-indicator/) | backend/usagePoller+index+session, webview/UsageIndicator+utils/usage+global.css | Three-bar limits indicator in the header (absorbing the old Account & usage button), fed by one poller per server process started from `startServer`: refresh once a minute while there is activity, pause after an idle hour, broadcast to every client. `usagePoller` is the only caller of the usage API - clients ask via `requestUsageRefresh` and get the snapshot unless the last attempt is over a minute old, so N panels clicking refresh cost one request ([common/rate-limited-external-apis.md](../common/rate-limited-external-apis.md)); also fixed a track painted in its own surface colour and an empty state that never said why ([common/ui-that-reads-as-broken.md](../common/ui-that-reads-as-broken.md)). Also fixed an off-by-one in its spec (2026-09-06): `reqAt - fetchedAt > 0` is one ms tighter than the real boundary (`>=`), and it only fails on a cold snapshot, so it dies on the first usage call of a suite and passes on every re-run |
| [model-refresh-retry-backoff/](model-refresh-retry-backoff/) | backend/modelData, backend/config | FIXED: a failed `/v1/models` fetch still stamped `modelDataUpdatedAt`, so a 200k fallback (5x-wrong percent on a 1M model) could hide for 24h; split into a success clock + a new `modelDataAttemptedAt` retrying hourly, keeping the old respawn protection |
| [session-active-marker/](session-active-marker/) | backend/channel, webview/SessionHistoryModal | Pulsing dot on sessions with a turn running now, server-derived (`listActiveSessions` = sessionId + live proc + `!cliDone`) and pushed to every client; integration spec verified green 2026-08-26 after its prompt was changed to hold the turn open with a Bash `sleep` (the old one raced the model, see [empty-1s-turn/](empty-1s-turn/)) |
| [pending-tool-pulse/](pending-tool-pulse/) | webview/ToolCall | FIXED: a resultless tool kept pulsing green after its turn ended (`finalizeBlocks` only reaches `state.streaming`, never a committed or disk-replayed message); `pending` now also requires `!sessionDone` |
| [browse-send-routing/](browse-send-routing/) | backend/channel, backend/session | FIXED (2 bugs): a send while browsing another session was injected into the stdin of the turn the client left; and resuming a session streaming in another entry showed no progress indicator and never updated. Both from handlers acting on the client's current entry instead of the one owning the session - `clientViewing` + `detachToBrowsedSession` + `attachToLiveSession`. Also fixed a harness race in its own spec (2026-09-06): a raw WS client attached its recorder after `await open`, so the `sessionLoaded` the server replays during the upgrade was dropped whenever it shared a TCP read with the handshake |
| [account-and-usage/](account-and-usage/) | backend/accountUsage, webview/AccountUsageModal | Account & Usage modal: live `/oauth/usage` API (primary) + stream fallback; two-phase load; 429 error surfacing; refresh + 60s cache |
| [streaming-and-input-ui/](streaming-and-input-ui/) | backend/cli, webview/input | Enable per-token streaming via `--include-partial-messages` + `stream_event` unwrap; drop model default; Send/Stop button icon restyle |
| [session-bar-and-log-autoscroll/](session-bar-and-log-autoscroll/) | webview/App+global.css, LogPanel, backend/sessions | Collapsible full-width session header (name + history + new-chat); `custom-title` rename precedence; fix log autoscroll breaking mid-stream (direction-based scroll detection) |
| [dialog-state-persistence/](dialog-state-persistence/) | webview/utils/dialogState, hooks/useDialogGeometry, SettingsModal | Persist each centered dialog's position/size/tab to `localStorage` (survives refresh) + Settings "Reset layout" button to wipe it; `clearDialogState()` and `useDialogGeometry.reset()` |
| [single-daemon-server/](single-daemon-server/) | backend/index+daemon, frontend/extension+ChatPanel, SettingsModal, chat.html | IMPLEMENTED: single always-on daemon on a fixed port (configurable `daemonPort`/`daemonIdleMs`) with idle self-exit; auto-spawned + connect-only extension (discovery file `~/.claude/argus-daemon.json`); browser-served UI; in-app restart; Network tab daemon-port randomize button + field seeded from the real running port |
| [token-spending-and-askuserquestion/](token-spending-and-askuserquestion/) | backend/cliHandler, webview/ThinkingBlock+StreamingTimer, e2e | Live token counts in StreamingTimer (message_start/delta -> token_update); collapsible ThinkingBlock with char-estimate; AskUserQuestion integration tests skipped (all models refuse in --print mode); --allowedTools fix; effort ?? fix |
| [panel-isolation-and-session-info/](panel-isolation-and-session-info/) | backend/channel+session+version, frontend/ChatPanel, SettingsModal | Per-panel `?panel=<uuid>` session entries (two panels no longer share one conversation; a reload rejoins its own); Info tab Client/Server versions with `(stale)` skew warning; session id + transcript path rows |
| [file-preview-navigation/](file-preview-navigation/) | webview/FileViewerModal+markdown+filePath, ToolCall | Follow relative markdown links inside the file previewer (frame stack + Back); link `!notes` paths correctly (both regexes, escape skips code spans); guard stringified `AskUserQuestion.questions`; hide "Open in editor" outside VS Code |
| [e2e-stabilization/](e2e-stabilization/) | playwright.config.ts, e2e/ | Fix 40-test cascade: workers: 2 caused resource exhaustion + concurrent argus.json writes; switched to workers: 1 (102 passed, ~7-13 min) |
| [e2e-config-guard/](e2e-config-guard/) | backend/index+config, playwright.config.ts, e2e/global-setup.ts | `/health` + `global-setup.ts` guard against a reused dev server reading/writing the wrong config; flattened integration timeout from 90s to the global 30s (108 passed, ~5 min) with a rare justified per-test exception |
| [stop-all-claude-button/](stop-all-claude-button/) | backend/cli+session, SettingsModal, e2e | Machine-wide "Stop all Claude CLI processes" button (Settings > Info), two-click arm/confirm; `killAllClaude()` via locale-independent `tasklist` CSV count + `taskkill /IM claude.exe` |
| [stop-then-send-race/](stop-then-send-race/) | backend/session+cliHandler+sessionState, e2e | Fix a message sent right after Stop being swallowed and the new turn ending in ~1s: the killed proc kept a writable stdin until its async `close`, so the send was taken for a mid-turn inject; detach before kill everywhere + a `detached()` guard on the proc handlers |
| [preview-modal-persistence/](preview-modal-persistence/) | webview/contexts/PreviewContext, ToolCall, filePath, App | Fix file/diff/image previews closing when the turn finishes: the open state lived in the ToolCall, which is unmounted when streaming blocks commit into a ChatMessage; hoisted to an App-level `PreviewProvider` stack |
| [stop-daemon-button/](stop-daemon-button/) | backend/index+session, SettingsModal, chat.html, e2e | "Stop daemon" button (Settings > Info): in-app `yarn daemon:stop` over WS, gated on `onIdleShutdown` so the dev server refuses; `userStopped` in chat.html stops the extension auto-respawning the daemon it was just asked to stop |
| [version-skew-direction/](version-skew-direction/) | webview/SettingsModal, e2e | Fix: version mismatch warning always blamed the Server row even when the daemon was newer; added numeric `compareVersions()` so `(stale)` lands on whichever side (Client or Server) is actually behind |
| [model-picker-refresh/](model-picker-refresh/) | backend/session+channel+modelData+daemon, webview/AccountUsageModal+InputArea | Fix stale current-model highlight (switch broadcasts were per-channel; config is now the single source of truth + global broadcast); family-based model descriptions; daemon records `daemonLastStartAt` and auto-refreshes model data daily |
| [usage-insights/](usage-insights/) | backend/usageInsights+accountUsage, webview/AccountUsageModal | Official "What's contributing to your limits usage?" section: local transcript scan mirroring the CLI's cost/behavior algorithm (Day/Week, attribution tables) + the Weekly Fable window from the API's `limits` array |
| [workspace-info-config/](workspace-info-config/) | backend/workspaceInfo+session, frontend/ChatPanel | Fix extension panels forgetting config-global model/effort/thinking on load: the VS_ONLY getInfo reply lacked the fields; single shared `buildWorkspaceInfo()` for both responders + integration spec |
| [code-block-copy-button/](code-block-copy-button/) | webview/utils/markdown, e2e | Fix the code-block copy button sliding out of view on horizontal scroll: the `<pre>` was both scroll container and positioning context, so the absolute button scrolled with the content ([common/overlay-controls-on-scrollable.md](../common/overlay-controls-on-scrollable.md)) |
| [context-window-percent/](context-window-percent/) | backend/cliHandler+modelData+accountUsage, webview/InputArea | Fix the context pill pegging at 100% on 1M models: the denominator was hardcoded 200k, now per-model via `contextWindowFor()` from `/v1/models` `max_input_tokens` (the CLI's own `contextWindow` lies for models newer than its bundle); output tokens dropped from the numerator |
| [url-linkified-as-path/](url-linkified-as-path/) | webview/utils/url+filePath+markdown, global.css, e2e | Fix a URL rendering as a **file** link (`https://host/ui/scripts/main.js` -> `/host/ui/scripts/main.js` underlined, preview of a nonexistent file): `URL_RE` is matched first and the hit rendered as an external link opened via `openExternal()`; the first pass made it plain text instead, superseded once the user clarified the links themselves are wanted; only bit code spans, fenced blocks and user messages, since remark-gfm autolinks URLs in prose |
| [session-name-task-tag/](session-name-task-tag/) | backend/sessions (investigation only) | Prefix session titles with the task tag (CON-45, `empty-1s-turn`): the title is the CLI's `ai-title` record, so tagging means appending a `custom-title` line; naive id-grep over transcripts is unusable (tool results inherit every ticket id in any file read), an engagement-scored probe (first-prompt/notes-written/URL signals, never `tool_result`) is right on all single-task sessions but needs a sweep-session guard; write-safety constraints drafted, nothing implemented |
| [dir-preview/](dir-preview/) | backend/filePreview, webview/FileViewerModal+PreviewContext+FolderList+fileIcon+filePath+markdown+path, e2e | A clicked directory path rendered `EISDIR` instead of a listing, and the link covered only `C:UsersAdmin.claude` rather than the folder the sentence named. Directory preview now reuses the Browse tab rows (files included, capped at 1000) with Seti-style file icons; the Windows path branch drops the extension requirement (slash branches tried and reverted - a URL path in prose is shaped exactly like a directory). Five pre-existing spec failures found and routed to their owning notes |
| [path-with-spaces-mention/](path-with-spaces-mention/) | backend/fileSearch+session, webview/InputArea+filePath+markdown | FIXED: `@Бискуб Константин Николаевич/…/Военный билет 12.jpg` highlighted only `@Бискуб` - and the CLI's own `@` parser stops at the first space too, so the file was **never attached** and the agent went hunting for it. `@"path with spaces"` is the form that works ([common/at-mentions.md](../common/at-mentions.md)); added an explorer-style `@` picker (one level at a time, folders first, drill in/out, recursive search, quoted insertion) over a new `searchFiles`/`fileList` message, and widened both path regexes for Unicode + spaces ([common/markdown-file-paths.md](../common/markdown-file-paths.md)). No e2e specs yet |
| [at-in-filename-truncates-link/](at-in-filename-truncates-link/) | webview/utils/filePath+markdown, e2e | FIXED: `…	elegramout@snowy_137.json` linked `…	elegramout` - an existing folder, so the click opened a listing and nothing looked broken. `@` added to both regexes, asymmetrically (anywhere on the Windows branch, directory-segment or leading-only on the slash branches, so `docs/john@corp.com` stays prose); audited over 1,471 transcripts / 60,653 blocks: 94 links added, all filesystem paths, 0 handles, 0 addresses. Left open: in prose remark-gfm autolinks a mid-segment `@` as `mailto:` before the linkifier runs |
| [no-response-requested/](no-response-requested/) | backend/cli+session+sessionState+cliHandler+sessions, e2e | FIXED: a real user message was answered `No response requested.` - and the model genuinely produced it (7 output tokens, `claude-opus-5`, token totals matching the UI exactly), copying a phrase it had seen five times in its own context. The CLI splices a `<synthetic>` assistant turn carrying that literal whenever a resumed transcript ends with an unanswered user message (`deserializeMessagesWithInterruptDetection`), which is what every Argus hard kill leaves behind, and it **sends that to the model** (proved by asking it to quote its own prior messages). Falsified over all 1,710 transcripts: 292 placeholders, 4 real emissions, **all** in sessions with placeholders, **0** in the 142 with 4 or fewer. Interrupting instead of killing does *not* repair the transcript (measured 3 ways) - the fix is that the splice only fires when the *last* message is a user message, so `handleStop` interrupts and **keeps the process** for reuse, burying the abandoned message under the next answer |
| [bg-task-indicators/](bg-task-indicators/) | webview/reducer+types+BackgroundTasksNote+ChatMessage, backend/session+cliHandler+sessionState, e2e | FIXED: a turn that launched exactly two background tasks rendered "2 of 9 background tasks still running" - the denominator counted every `task_started` since a per-send reset while the tasks themselves outlive turns, so it described a window unrelated to the message it hung on. Counter deleted; the note is now numerator-only. Two candidate mechanisms for the 9 were probed against a live CLI and **both died** (`task_started` is not re-emitted for still-running tasks; `task_updated` fires at completion, not incrementally), so its provenance is unresolved - what is established is that the number was not one the reader could name |
| [connected-clients/](connected-clients/) | backend/clients+channel+index+session, webview/ClientsModal+SettingsModal+shared/dataTable, e2e | The Settings "Active connections" number showed a count and nothing else. Added a panel listing every WebSocket client the server serves (host+device, address with a `remote` badge, workspace, session, mid-turn, age), fed by the upgrade request (captured at accept time) joined with the session registry, on the same `readyState===1` filter as the count so the two cannot disagree. Per-row disconnect closes with code **4001**, which `ws-bridge.js` alone does not retry - otherwise the button hands the connection straight back - and the closed panel swaps its pulsing dot for a Reconnect pill. `display:flex` on a `<td>` broke every row onto two lines and passed every assertion; only looking at it found that |
| [remote-access-auth/](remote-access-auth/) | backend/auth+index+session, webview/ws-bridge+LoginScreen+SettingsModal+App+reducer, shims, e2e | Closes security-audit #3. A remote peer must sign in; loopback and the VS Code webview never do. **The gate keys off the peer address, not the Origin** - probed first: with `allowNetworkAccess:false` a LAN browser got 403 while a client that simply omitted the header CONNECTED, from anywhere, and `/nonce` was handed to anyone. scrypt+salt in a 600-mode `argus-auth.json` (never `argus.json`, which `updateSettings` bulk-merges), in-memory sessions, 5-failure lockout doubling to 15 min, revocation on password change. No password now means **refused**, not open. The login screen tested green and was still invisible in a real browser: `auth_required` fires before React mounts and the dedupe stopped it firing again |
