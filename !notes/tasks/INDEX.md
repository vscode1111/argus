# Investigation Notes Index

| Folder | Module | Summary |
|--------|--------|---------|
| [account-and-usage/](account-and-usage/) | backend/accountUsage, webview/AccountUsageModal | Account & Usage modal: live `/oauth/usage` API (primary) + stream fallback; two-phase load; 429 error surfacing; refresh + 60s cache |
| [streaming-and-input-ui/](streaming-and-input-ui/) | backend/cli, webview/input | Enable per-token streaming via `--include-partial-messages` + `stream_event` unwrap; drop model default; Send/Stop button icon restyle |
| [session-bar-and-log-autoscroll/](session-bar-and-log-autoscroll/) | webview/App+global.css, LogPanel, backend/sessions | Collapsible full-width session header (name + history + new-chat); `custom-title` rename precedence; fix log autoscroll breaking mid-stream (direction-based scroll detection) |
| [dialog-state-persistence/](dialog-state-persistence/) | webview/utils/dialogState, hooks/useDialogGeometry, SettingsModal | Persist each centered dialog's position/size/tab to `localStorage` (survives refresh) + Settings "Reset layout" button to wipe it; `clearDialogState()` and `useDialogGeometry.reset()` |
| [single-daemon-server/](single-daemon-server/) | backend/index+daemon, frontend/extension+ChatPanel, SettingsModal, chat.html | IMPLEMENTED: single always-on daemon on a fixed port (configurable `daemonPort`/`daemonIdleMs`) with idle self-exit; auto-spawned + connect-only extension (discovery file `~/.claude/argus-daemon.json`); browser-served UI; in-app restart; Network tab daemon-port randomize button + field seeded from the real running port |
| [token-spending-and-askuserquestion/](token-spending-and-askuserquestion/) | backend/cliHandler, webview/ThinkingBlock+StreamingTimer, e2e | Live token counts in StreamingTimer (message_start/delta -> token_update); collapsible ThinkingBlock with char-estimate; AskUserQuestion integration tests skipped (all models refuse in --print mode); --allowedTools fix; effort ?? fix |
| [panel-isolation-and-session-info/](panel-isolation-and-session-info/) | backend/channel+session+version, frontend/ChatPanel, SettingsModal | Per-panel `?panel=<uuid>` session entries (two panels no longer share one conversation; a reload rejoins its own); Info tab Client/Server versions with `(stale)` skew warning; session id + transcript path rows |
| [file-preview-navigation/](file-preview-navigation/) | webview/FileViewerModal+markdown+filePath, ToolCall | Follow relative markdown links inside the file previewer (frame stack + Back); link `!notes` paths correctly (both regexes, escape skips code spans); guard stringified `AskUserQuestion.questions`; hide "Open in editor" outside VS Code |
| [e2e-stabilization/](e2e-stabilization/) | playwright.config.ts, e2e/ | Fix 40-test cascade: workers: 2 caused resource exhaustion + concurrent argus.json writes; switched to workers: 1 (102 passed, ~7-13 min) |
| [e2e-config-guard/](e2e-config-guard/) | backend/index+config, playwright.config.ts, e2e/global-setup.ts | `/health` + `global-setup.ts` guard against a reused dev server reading/writing the wrong config; flattened integration timeout from 90s to the global 30s (108 passed, ~5 min) with a rare justified per-test exception |
| [stop-all-claude-button/](stop-all-claude-button/) | backend/cli+session, SettingsModal, e2e | Machine-wide "Stop all Claude CLI processes" button (Settings > Info), two-click arm/confirm; `killAllClaude()` via locale-independent `tasklist` CSV count + `taskkill /IM claude.exe` |
| [version-skew-direction/](version-skew-direction/) | webview/SettingsModal, e2e | Fix: version mismatch warning always blamed the Server row even when the daemon was newer; added numeric `compareVersions()` so `(stale)` lands on whichever side (Client or Server) is actually behind |
| [model-picker-refresh/](model-picker-refresh/) | backend/session+channel+modelData+daemon, webview/AccountUsageModal+InputArea | Fix stale current-model highlight (switch broadcasts were per-channel; config is now the single source of truth + global broadcast); family-based model descriptions; daemon records `daemonLastStartAt` and auto-refreshes model data daily |
| [usage-insights/](usage-insights/) | backend/usageInsights+accountUsage, webview/AccountUsageModal | Official "What's contributing to your limits usage?" section: local transcript scan mirroring the CLI's cost/behavior algorithm (Day/Week, attribution tables) + the Weekly Fable window from the API's `limits` array |
| [workspace-info-config/](workspace-info-config/) | backend/workspaceInfo+session, frontend/ChatPanel | Fix extension panels forgetting config-global model/effort/thinking on load: the VS_ONLY getInfo reply lacked the fields; single shared `buildWorkspaceInfo()` for both responders + integration spec |
| [code-block-copy-button/](code-block-copy-button/) | webview/utils/markdown, e2e | Fix the code-block copy button sliding out of view on horizontal scroll: the `<pre>` was both scroll container and positioning context, so the absolute button scrolled with the content ([common/overlay-controls-on-scrollable.md](../common/overlay-controls-on-scrollable.md)) |
| [context-window-percent/](context-window-percent/) | backend/cliHandler+modelData+accountUsage, webview/InputArea | Fix the context pill pegging at 100% on 1M models: the denominator was hardcoded 200k, now per-model via `contextWindowFor()` from `/v1/models` `max_input_tokens` (the CLI's own `contextWindow` lies for models newer than its bundle); output tokens dropped from the numerator |
